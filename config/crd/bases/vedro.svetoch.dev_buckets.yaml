---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: buckets.vedro.svetoch.dev
spec:
  group: vedro.svetoch.dev
  names:
    categories:
    - vedro
    kind: Bucket
    listKind: BucketList
    plural: buckets
    singular: bucket
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.providerRef.name
      name: Provider
      type: string
    - jsonPath: .status.externalName
      name: Bucket
      type: string
    - jsonPath: .spec.location
      name: Location
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Bucket is the Schema for the buckets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              cloudSpecificConfig:
                description: |-
                  CloudSpecificConfig contains provider-specific bucket settings that do not
                  have a cloud-agnostic equivalent in BucketSpec.

                  These settings are applied only when the selected provider matches the
                  corresponding provider config field. For example, Gcp is used only for GCS
                  buckets.
                properties:
                  gcp:
                    description: GCP-specific bucket configuration.
                    properties:
                      softDeletePolicy:
                        properties:
                          retentionDuration:
                            default: 168h
                            description: |-
                              RetentionDuration is how long deleted objects are retained before permanent deletion.
                              Must be a whole number of days for GCS.
                              Example: "168h" for 7 days.
                            type: string
                            x-kubernetes-validations:
                            - message: retentionDuration must be 0 or a whole number
                                of days
                              rule: duration(self) >= duration('0s') && duration(self).getSeconds()
                                % 86400 == 0
                        required:
                        - retentionDuration
                        type: object
                    type: object
                  yc:
                    description: |-
                      Yandex cloud-specific bucket configuration.
                      Reserved for future Yandex Cloud extensions.
                    type: object
                type: object
              deletionPolicy:
                default: Retain
                description: |-
                  DeletionPolicy controls what happens to the external bucket
                  when this Kubernetes object is deleted.
                enum:
                - Delete
                - Retain
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels are cloud provider labels/tags applied to the
                  bucket.
                type: object
              lifecycle:
                description: Lifecycle configures object lifecycle rules.
                properties:
                  rules:
                    items:
                      properties:
                        action:
                          description: Action describes what happens to matching objects.
                          enum:
                          - Delete
                          type: string
                        ageDays:
                          description: AgeDays matches objects older than this many
                            days.
                          format: int64
                          minimum: 1
                          type: integer
                        enabled:
                          default: true
                          description: Enabled controls whether this lifecycle rule
                            should be active.
                          type: boolean
                        name:
                          description: Name is a stable identifier for this lifecycle
                            rule.
                          minLength: 1
                          type: string
                      required:
                      - action
                      - enabled
                      type: object
                    type: array
                type: object
              location:
                description: |-
                  Location is the cloud provider location/region.

                  Examples:
                  - GCP: europe-west1
                  - Yandex: ru-central1
                minLength: 1
                type: string
              name:
                description: Name is the real cloud provider bucket name.
                minLength: 1
                type: string
              providerRef:
                description: ProviderRef references the ProviderConfig used to manage
                  this bucket.
                properties:
                  name:
                    description: Name is the name of the ProviderConfig.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              publicAccessPrevention:
                description: PublicAccessPrevention controls whether the bucket may
                  be publicly accessible.
                type: boolean
              storageClass:
                default: Standard
                description: bucket storage class
                enum:
                - Standard
                - Warm
                - Cold
                - Ice
                type: string
              unsupportedFeaturePolicy:
                default: Fail
                description: |-
                  UnsupportedFeaturePolicy controls what the controller does when
                  the selected provider does not support a requested feature.
                enum:
                - Fail
                - Warn
                type: string
              versioning:
                description: Versioning configures object versioning.
                properties:
                  enabled:
                    description: Enabled controls bucket object versioning.
                    type: boolean
                required:
                - enabled
                type: object
            required:
            - location
            - providerRef
            type: object
          status:
            properties:
              applied:
                description: Applied is the applied bucket attributes
                properties:
                  cloudSpecificConfig:
                    properties:
                      gcp:
                        description: GCP-specific bucket configuration.
                        properties:
                          softDeletePolicy:
                            properties:
                              retentionDuration:
                                default: 168h
                                description: |-
                                  RetentionDuration is how long deleted objects are retained before permanent deletion.
                                  Must be a whole number of days for GCS.
                                  Example: "168h" for 7 days.
                                type: string
                                x-kubernetes-validations:
                                - message: retentionDuration must be 0 or a whole
                                    number of days
                                  rule: duration(self) >= duration('0s') && duration(self).getSeconds()
                                    % 86400 == 0
                            required:
                            - retentionDuration
                            type: object
                        type: object
                      yc:
                        description: |-
                          Yandex cloud-specific bucket configuration.
                          Reserved for future Yandex Cloud extensions.
                        type: object
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    type: object
                  lifecycle:
                    properties:
                      rules:
                        items:
                          properties:
                            action:
                              description: Action describes what happens to matching
                                objects.
                              enum:
                              - Delete
                              type: string
                            ageDays:
                              description: AgeDays matches objects older than this
                                many days.
                              format: int64
                              minimum: 1
                              type: integer
                            enabled:
                              default: true
                              description: Enabled controls whether this lifecycle
                                rule should be active.
                              type: boolean
                            name:
                              description: Name is a stable identifier for this lifecycle
                                rule.
                              minLength: 1
                              type: string
                          required:
                          - action
                          - enabled
                          type: object
                        type: array
                    type: object
                  publicAccessPrevention:
                    type: boolean
                  storageClass:
                    type: string
                  versioning:
                    properties:
                      enabled:
                        description: Enabled controls bucket object versioning.
                        type: boolean
                    required:
                    - enabled
                    type: object
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the bucket state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              externalName:
                description: ExternalName is the provider-side bucket name.
                type: string
              location:
                description: Location is the provider-side bucket region.
                type: string
              observedGeneration:
                description: ObservedGeneration is the latest metadata.generation
                  observed by the controller.
                format: int64
                type: integer
              observedProvider:
                description: Provider used for this bucket
                type: string
              unsupported:
                description: List of unsupported features set on Bucket resource
                items:
                  properties:
                    field:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                  required:
                  - field
                  - message
                  - reason
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
